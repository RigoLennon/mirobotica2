 <%= form_with(model: category, local: true) do |form| %>
      <% if category.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

            <ul>
              <% category.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>

      <div class="col-sm-12">
        <%= form.label :title, "Título", class:"h3" %>
        <%= form.text_field :title, id: :category_title, class:"form-control" %>
      </div>

      <div class="col-sm-12">
        <%= form.label :color, class:"h3" %> <br>
        <%= form.color_field :color, id: :category_color %>
      </div>
      <br>

      <div class="col-md-12">
        <%= form.label :image, "Imagen",class:"h3" %>
        <%= form.file_field :image,class:"btn btn-success " %> <br>
      </div>

      <div class="col-md-12 center-xs">
        <%= form.submit "Crear categoria", class:"btn btn-success"%>
      </div>
  <% end %>